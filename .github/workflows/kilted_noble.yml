name: ros2-kilted-noble

on:
  push

jobs:
  ros2_kilted_noble_ci:
    name: kilted (noble)
    uses: ./.github/workflows/ros2.yml
    with:
      ROS_DISTRO: kilted
      ROS_REPO: testing
      OS_NAME: ubuntu
      OS_CODE_NAME: noble
      ALLOW_FAIL: false
      UPSTREAM_WORKSPACE: ci/upstream.repos
